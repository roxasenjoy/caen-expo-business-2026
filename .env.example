# Copier ce fichier en .env et remplir les valeurs avant de lancer docker compose up

APP_ENV=production

# Cle aleatoire utilisee par PHP pour signer les sessions (openssl rand -base64 32)
SESSION_SECRET=changeme

# MySQL
MYSQL_ROOT_PASSWORD=changeme_root
MYSQL_PASSWORD=changeme_app
